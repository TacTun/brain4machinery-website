name = "brain4machinery-website"
compatibility_date = "2025-05-01"
pages_build_output_dir = "dist"

# D1 binding for the contact-form submissions table.
# Database created via `npx wrangler d1 create brain4machinery-contact`.
# Schema lives in schema.sql; apply with:
#   npx wrangler d1 execute brain4machinery-contact --file=schema.sql --remote
[[d1_databases]]
binding = "DB"
database_name = "brain4machinery-contact"
database_id = "617a6d18-f83a-4577-b4de-f9642fa37673"
